import React from 'react';
import { Layout } from '../components/Layout';

const NotFoundPage: React.FC = () => {
  return (
    <Layout>
      <p>
        I&apos;m terribly sorry to tell you this, but an error has occurred.
      </p>
    </Layout>
  );
};

export default NotFoundPage;
